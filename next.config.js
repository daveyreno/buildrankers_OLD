/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ouleipcuazzmcbojpbet.supabase.co'], // Replace 'example.com' with the domain that hosts your images
    },
};

module.exports = nextConfig;
